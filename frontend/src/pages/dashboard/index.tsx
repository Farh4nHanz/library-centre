import { Loader } from "@/components/loader";

const DashboardPage = () => {
  return (
    <div className="h-[200vh] w-full flex justify-center items-center">
      <Loader color="green" size="xxl" />
    </div>
  );
};

export default DashboardPage;
